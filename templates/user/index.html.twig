{% extends 'base.html.twig' %}

{% block title %}Liste des utilisateurs{% endblock %}

{% block body %}
    <div class="container">
        <nav class="nav navbar">
            <div class="col-sm-6">
                <h1>Liste des utilisateurs</h1>
            </div>   
            <a class="btn-lg btn btn-success" href="{{ path('back_user_create') }}">Ajouter</a>
        </nav>

        <table class="table" >
            <thead>
                <tr >
                    <th>Id</th>
                    <th>Pseudo</th>
                    <th>Image</th>
                    <th>Email</th>
                    <th>Password</th>
                    <th>Rôle</th>
                    <th>Active</th>
                    <th>Actions</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
            {% for user in users %}
                <tr>
                    <td>{{ user.id }}</td>
                    <td>{{ user.username }}</td>
                    <td>{{ user.image }}</td>
                    <td>{{ user.email }}</td>
                    <td>{{ user.password }}</td>
                    <td>{{ user.role }}</td>
                    <td>{{ user.active }}</td>
                    <td>
                        <a  class="btn btn-sm btn-warning" href="{{ path('back_user_update', {'id': user.id}) }}">Editer</a>
                    </td>
                    <td>
                        {{ include('user/_delete_form.html.twig') }}
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
