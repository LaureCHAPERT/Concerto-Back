{{ form_start(form) }}

    {{ form_row(form.name) }}
    {{ form_row(form.description) }}
    {{ form_row(form.date) }}
    {{ form_row(form.hour) }}
    {{ form_row(form.price) }}
    {{ form_row(form.image) }}
    {{ form_row(form.linkTicketing) }}
    {{ form_row(form.genres) }}
    <button class="btn btn-success">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
