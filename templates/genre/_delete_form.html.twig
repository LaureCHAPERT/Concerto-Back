<form method="post" action="{{ path('back_genre_delete', {'id': genre.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ça ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ genre.id) }}">
<button class="btn btn-danger ">Supprimer</button>
</form>
